services:
  terve:
    image: lehmann314159/terve:latest
    container_name: terve
    ports:
      - "3001:3001"
    volumes:
      - terve-data:/app/data
    environment:
      - PORT=3001
      - DATABASE_PATH=/app/data/terve.db
    restart: unless-stopped

volumes:
  terve-data: